import { ReactElement } from "react";

const Section = (): ReactElement => {
  return <div className="w-[600px] h-fit p-6 bg-white rounded-lg shadow-md flex flex-col gap-6">
    <h3></h3>
  </div>;
};

export default Section;
